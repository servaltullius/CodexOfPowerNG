const test = require("node:test");
const assert = require("node:assert/strict");
const fs = require("node:fs");
const path = require("node:path");

const viewPath = path.join(__dirname, "..", "PrismaUI", "views", "codexofpowerng", "index.html");
const modulePath = path.join(__dirname, "..", "PrismaUI", "views", "codexofpowerng", "ui_i18n.js");

const html = fs.readFileSync(viewPath, "utf8");
const moduleSource = fs.readFileSync(modulePath, "utf8");
const mod = require(modulePath);

test("view loads ui_i18n module before inline bootstrap", () => {
  assert.match(html, /<script src="ui_i18n\.js"><\/script>/);
});

test("ui_i18n module exports message tables and translator factory", () => {
  assert.equal(typeof mod.createTranslator, "function");
  assert.equal(typeof mod.normalizeLanguage, "function");
  assert.equal(typeof mod.MESSAGES.en["status.lotdGate"], "string");
  assert.equal(typeof mod.MESSAGES.ko["settings.lotdGateWarnTitle"], "string");
});

test("index delegates translation helpers to ui_i18n module", () => {
  assert.match(html, /const uiI18nApi = .*window\.COPNGUII18n/);
  assert.match(html, /const \{ t, tFmt \} = uiI18nApi\.createTranslator\(/);
  assert.doesNotMatch(html, /const I18N = \{/);
});

test("ui_i18n module keeps LOTD and formatting messages in dedicated dictionaries", () => {
  assert.match(moduleSource, /"status\.lotdGate":\s*"LOTD Gate"/);
  assert.match(moduleSource, /"status\.lotdGate":\s*"LOTD 게이트"/);
  assert.match(moduleSource, /"settings\.lotdGateWarnTitle":\s*"LOTD gate warning"/);
  assert.match(moduleSource, /"settings\.lotdGateWarnBody":\s*"LOTD Display gate is enabled, but TCC lists are unavailable\./);
  assert.match(moduleSource, /"build\.focusTitle":\s*"Focused Option"/);
  assert.match(moduleSource, /"build\.focusTitle":\s*"선택 옵션"/);
  assert.match(moduleSource, /"build\.altarTitle":\s*"Build Shrine"/);
  assert.match(moduleSource, /"build\.altarTitle":\s*"빌드 제단"/);
  assert.match(moduleSource, /"build\.compatibleSlots":\s*"Compatible Slots: \{slots\}"/);
  assert.match(moduleSource, /"build\.compatibleSlots":\s*"호환 슬롯: \{slots\}"/);
  assert.match(moduleSource, /"build\.attack\.vitals\.title":\s*"Vitals"/);
  assert.match(moduleSource, /"build\.attack\.vitals\.description":\s*"\+3% critical chance while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.pinpoint\.title":\s*"Pinpoint"/);
  assert.match(moduleSource, /"build\.attack\.pinpoint\.description":\s*"\+2% critical chance while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.brawler\.title":\s*"Brawler"/);
  assert.match(moduleSource, /"build\.attack\.brawler\.description":\s*"\+0\.2 unarmed damage while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.reserve\.title":\s*"Reserve"/);
  assert.match(moduleSource, /"build\.attack\.reserve\.description":\s*"\+2 max stamina while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.secondwind\.title":\s*"Second Wind"/);
  assert.match(moduleSource, /"build\.attack\.secondwind\.description":\s*"\+0\.1 stamina regeneration while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.destruction\.title":\s*"Destruction"/);
  assert.match(moduleSource, /"build\.attack\.destruction\.description":\s*"\+0\.5 destruction cost reduction while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.endurance\.title":\s*"Endurance"/);
  assert.match(moduleSource, /"build\.defense\.endurance\.description":\s*"\+15 max health while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.bulwark\.title":\s*"Bulwark"/);
  assert.match(moduleSource, /"build\.defense\.bulwark\.description":\s*"\+8 armor rating while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.recovery\.title":\s*"Recovery"/);
  assert.match(moduleSource, /"build\.defense\.recovery\.description":\s*"\+0\.02 health regeneration while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.restoration\.title":\s*"Restoration"/);
  assert.match(moduleSource, /"build\.defense\.restoration\.description":\s*"\+0\.5 restoration cost reduction while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.reprisal\.title":\s*"Reprisal"/);
  assert.match(moduleSource, /"build\.defense\.reprisal\.description":\s*"\+0\.3 reflected damage while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.alteration\.title":\s*"Alteration"/);
  assert.match(moduleSource, /"build\.defense\.alteration\.description":\s*"\+0\.5 alteration cost reduction while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.warding\.title":\s*"Magic Ward"/);
  assert.match(moduleSource, /"build\.defense\.warding\.description":\s*"\+0\.75% magic resist while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.fireward\.title":\s*"Fire Ward"/);
  assert.match(moduleSource, /"build\.defense\.fireward\.description":\s*"\+1% fire resist while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.absorption\.title":\s*"Absorption"/);
  assert.match(moduleSource, /"build\.defense\.absorption\.description":\s*"\+0\.4% spell absorption chance while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.smithing\.title":\s*"Smithing"/);
  assert.match(moduleSource, /"build\.utility\.smithing\.description":\s*"\+0\.5 smithing effectiveness while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.alchemy\.title":\s*"Alchemy"/);
  assert.match(moduleSource, /"build\.utility\.alchemy\.description":\s*"\+0\.5 alchemy effectiveness while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.enchanting\.title":\s*"Enchanting"/);
  assert.match(moduleSource, /"build\.utility\.enchanting\.description":\s*"\+0\.5 enchanting effectiveness while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.magicka\.title":\s*"Magicka Well"/);
  assert.match(moduleSource, /"build\.utility\.magicka\.description":\s*"\+2 max magicka while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.meditation\.title":\s*"Meditation"/);
  assert.match(moduleSource, /"build\.utility\.meditation\.description":\s*"\+0\.1 magicka regeneration while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.hauler\.title":\s*"Hauler"/);
  assert.match(moduleSource, /"build\.utility\.hauler\.description":\s*"\+15 carry weight while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.mobility\.title":\s*"Mobility"/);
  assert.match(moduleSource, /"build\.utility\.mobility\.description":\s*"\+3% movement speed while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.wayfinder\.title":\s*"Wayfinder"/);
  assert.match(moduleSource, /"build\.utility\.wayfinder\.description":\s*"\+1\.5% movement speed while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.echo\.title":\s*"Echo"/);
  assert.match(moduleSource, /"build\.utility\.echo\.description":\s*"Shout cooldown recovers 2% faster while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.sneak\.title":\s*"Sneak"/);
  assert.match(moduleSource, /"build\.utility\.sneak\.description":\s*"\+0\.5 sneak effectiveness while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.lockpicking\.title":\s*"Lockpicking"/);
  assert.match(moduleSource, /"build\.utility\.lockpicking\.description":\s*"\+0\.5 lockpicking effectiveness while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.pickpocket\.title":\s*"Pickpocket"/);
  assert.match(moduleSource, /"build\.utility\.pickpocket\.description":\s*"\+0\.5 pickpocket effectiveness while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.conjuration\.title":\s*"Conjuration"/);
  assert.match(moduleSource, /"build\.utility\.conjuration\.description":\s*"\+0\.5 conjuration cost reduction while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.illusion\.title":\s*"Illusion"/);
  assert.match(moduleSource, /"build\.utility\.illusion\.description":\s*"\+0\.5 illusion cost reduction while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.vitals\.title":\s*"급소"/);
  assert.match(moduleSource, /"build\.attack\.vitals\.description":\s*"슬롯 활성 시 치명타 확률이 3% 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.pinpoint\.title":\s*"정조준"/);
  assert.match(moduleSource, /"build\.attack\.pinpoint\.description":\s*"슬롯 활성 시 치명타 확률이 2% 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.brawler\.title":\s*"투사"/);
  assert.match(moduleSource, /"build\.attack\.brawler\.description":\s*"슬롯 활성 시 맨주먹 공격력이 0\.2 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.reserve\.title":\s*"기력"/);
  assert.match(moduleSource, /"build\.attack\.reserve\.description":\s*"슬롯 활성 시 최대 스태미나가 2 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.secondwind\.title":\s*"투지"/);
  assert.match(moduleSource, /"build\.attack\.secondwind\.description":\s*"슬롯 활성 시 스태미나 재생이 0\.1 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.destruction\.title":\s*"파괴"/);
  assert.match(moduleSource, /"build\.attack\.destruction\.description":\s*"슬롯 활성 시 파괴 주문 비용이 0\.5 감소합니다\."/);
  assert.match(moduleSource, /"build\.defense\.endurance\.title":\s*"인내"/);
  assert.match(moduleSource, /"build\.defense\.endurance\.description":\s*"슬롯 활성 시 최대 체력이 15 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.bulwark\.title":\s*"방벽"/);
  assert.match(moduleSource, /"build\.defense\.bulwark\.description":\s*"슬롯 활성 시 방어력이 8 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.recovery\.title":\s*"회복력"/);
  assert.match(moduleSource, /"build\.defense\.recovery\.description":\s*"슬롯 활성 시 체력 재생이 0\.02 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.restoration\.title":\s*"회복"/);
  assert.match(moduleSource, /"build\.defense\.restoration\.description":\s*"슬롯 활성 시 회복 주문 비용이 0\.5 감소합니다\."/);
  assert.match(moduleSource, /"build\.defense\.reprisal\.title":\s*"반격"/);
  assert.match(moduleSource, /"build\.defense\.reprisal\.description":\s*"슬롯 활성 시 반사 피해가 0\.3 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.alteration\.title":\s*"변화"/);
  assert.match(moduleSource, /"build\.defense\.alteration\.description":\s*"슬롯 활성 시 변화 주문 비용이 0\.5 감소합니다\."/);
  assert.match(moduleSource, /"build\.defense\.warding\.title":\s*"마법결계"/);
  assert.match(moduleSource, /"build\.defense\.warding\.description":\s*"슬롯 활성 시 마법 저항이 0\.75% 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.fireward\.title":\s*"화염결계"/);
  assert.match(moduleSource, /"build\.defense\.fireward\.description":\s*"슬롯 활성 시 화염 저항이 1% 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.absorption\.title":\s*"주문흡수"/);
  assert.match(moduleSource, /"build\.defense\.absorption\.description":\s*"슬롯 활성 시 주문 흡수 확률이 0\.4% 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.smithing\.title":\s*"대장"/);
  assert.match(moduleSource, /"build\.utility\.smithing\.description":\s*"슬롯 활성 시 대장 효율이 0\.5 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.alchemy\.title":\s*"연금"/);
  assert.match(moduleSource, /"build\.utility\.alchemy\.description":\s*"슬롯 활성 시 연금 효율이 0\.5 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.enchanting\.title":\s*"부여"/);
  assert.match(moduleSource, /"build\.utility\.enchanting\.description":\s*"슬롯 활성 시 부여 효율이 0\.5 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.magicka\.title":\s*"마력"/);
  assert.match(moduleSource, /"build\.utility\.magicka\.description":\s*"슬롯 활성 시 최대 매지카가 2 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.meditation\.title":\s*"명상"/);
  assert.match(moduleSource, /"build\.utility\.meditation\.description":\s*"슬롯 활성 시 매지카 재생이 0\.1 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.hauler\.title":\s*"짐꾼"/);
  assert.match(moduleSource, /"build\.utility\.hauler\.description":\s*"슬롯 활성 시 소지 한도가 15 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.mobility\.title":\s*"기동"/);
  assert.match(moduleSource, /"build\.utility\.mobility\.description":\s*"슬롯 활성 시 이동 속도가 3% 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.wayfinder\.title":\s*"길잡이"/);
  assert.match(moduleSource, /"build\.utility\.wayfinder\.description":\s*"슬롯 활성 시 이동 속도가 1\.5% 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.echo\.title":\s*"메아리"/);
  assert.match(moduleSource, /"build\.utility\.echo\.description":\s*"슬롯 활성 시 함성 재사용 대기시간이 2% 빨라집니다\."/);
  assert.match(moduleSource, /"build\.utility\.sneak\.title":\s*"은신"/);
  assert.match(moduleSource, /"build\.utility\.sneak\.description":\s*"슬롯 활성 시 은신 효율이 0\.5 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.lockpicking\.title":\s*"자물쇠"/);
  assert.match(moduleSource, /"build\.utility\.lockpicking\.description":\s*"슬롯 활성 시 자물쇠 해제 효율이 0\.5 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.pickpocket\.title":\s*"소매치기"/);
  assert.match(moduleSource, /"build\.utility\.pickpocket\.description":\s*"슬롯 활성 시 소매치기 효율이 0\.5 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.conjuration\.title":\s*"소환"/);
  assert.match(moduleSource, /"build\.utility\.conjuration\.description":\s*"슬롯 활성 시 소환 주문 비용이 0\.5 감소합니다\."/);
  assert.match(moduleSource, /"build\.utility\.illusion\.title":\s*"환영"/);
  assert.match(moduleSource, /"build\.utility\.illusion\.description":\s*"슬롯 활성 시 환영 주문 비용이 0\.5 감소합니다\."/);
  assert.match(moduleSource, /"build\.utility\.barter\.description":\s*"슬롯 활성 시 상점 구매\/판매 가격이 10% 유리해집니다\."/);
  assert.match(moduleSource, /return base\.replace/);
});

test("translator normalizes language and formats variables", () => {
  const translator = mod.createTranslator({ getLanguage: () => "ko" });
  assert.equal(translator.t("btn.save"), "저장");
  assert.equal(translator.t("build.attack.pinpoint.title"), "정조준");
  assert.equal(translator.t("build.attack.reserve.title"), "기력");
  assert.equal(translator.t("build.defense.warding.title"), "마법결계");
  assert.equal(translator.t("build.defense.recovery.title"), "회복력");
  assert.equal(translator.t("build.utility.smithing.title"), "대장");
  assert.equal(translator.t("build.utility.echo.title"), "메아리");
  assert.equal(translator.t("build.utility.wayfinder.description"), "슬롯 활성 시 이동 속도가 1.5% 증가합니다.");
  assert.equal(translator.tFmt("rewards.more", "+{n}개 더", { n: 5 }), "+5개 더");
  assert.equal(mod.normalizeLanguage("jp"), "en");
});
