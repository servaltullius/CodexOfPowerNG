const test = require("node:test");
const assert = require("node:assert/strict");
const fs = require("node:fs");
const path = require("node:path");

const viewPath = path.join(__dirname, "..", "PrismaUI", "views", "codexofpowerng", "index.html");
const modulePath = path.join(__dirname, "..", "PrismaUI", "views", "codexofpowerng", "ui_i18n.js");

const html = fs.readFileSync(viewPath, "utf8");
const moduleSource = fs.readFileSync(modulePath, "utf8");
const mod = require(modulePath);

test("view loads ui_i18n module before inline bootstrap", () => {
  assert.match(html, /<script src="ui_i18n\.js"><\/script>/);
});

test("ui_i18n module exports message tables and translator factory", () => {
  assert.equal(typeof mod.createTranslator, "function");
  assert.equal(typeof mod.normalizeLanguage, "function");
  assert.equal(typeof mod.MESSAGES.en["status.lotdGate"], "string");
  assert.equal(typeof mod.MESSAGES.ko["settings.lotdGateWarnTitle"], "string");
});

test("index delegates translation helpers to ui_i18n module", () => {
  assert.match(html, /const uiI18nApi = .*window\.COPNGUII18n/);
  assert.match(html, /const \{ t, tFmt \} = uiI18nApi\.createTranslator\(/);
  assert.doesNotMatch(html, /const I18N = \{/);
});

test("ui_i18n module keeps LOTD and formatting messages in dedicated dictionaries", () => {
  assert.match(moduleSource, /"status\.lotdGate":\s*"LOTD Gate"/);
  assert.match(moduleSource, /"status\.lotdGate":\s*"LOTD 게이트"/);
  assert.match(moduleSource, /"settings\.lotdGateWarnTitle":\s*"LOTD gate warning"/);
  assert.match(moduleSource, /"settings\.lotdGateWarnBody":\s*"LOTD Display gate is enabled, but TCC lists are unavailable\./);
  assert.match(moduleSource, /"build\.focusTitle":\s*"Focused Option"/);
  assert.match(moduleSource, /"build\.focusTitle":\s*"선택 옵션"/);
  assert.match(moduleSource, /"build\.altarTitle":\s*"Build Shrine"/);
  assert.match(moduleSource, /"build\.altarTitle":\s*"빌드 제단"/);
  assert.match(moduleSource, /"build\.compatibleSlots":\s*"Compatible Slots: \{slots\}"/);
  assert.match(moduleSource, /"build\.compatibleSlots":\s*"호환 슬롯: \{slots\}"/);
  assert.match(moduleSource, /"build\.help":\s*"Record count stays visible for collection motivation, while weighted build points unlock options and linearly scale slotted effects\."/);
  assert.match(moduleSource, /"build\.help":\s*"기록 수는 수집 동기용으로 그대로 남기고, 가중치가 적용된 빌드 포인트로 옵션 해금과 슬롯 효과의 선형 성장을 처리합니다\."/);
  assert.match(moduleSource, /"build\.summaryTier":\s*"Tier \{tier\}"/);
  assert.match(moduleSource, /"build\.summaryTier":\s*"단계 \{tier\}"/);
  assert.match(moduleSource, /"build\.pointsSummaryLabel":\s*"\{points\} pt"/);
  assert.match(moduleSource, /"build\.summaryNextTier":\s*"\{score\} pt to next tier"/);
  assert.match(moduleSource, /"build\.summaryNextTier":\s*"다음 단계까지 \{score\} pt"/);
  assert.match(moduleSource, /"build\.currentEffectLabel":\s*"Current Effect"/);
  assert.match(moduleSource, /"build\.currentEffectLabel":\s*"현재 효과"/);
  assert.match(moduleSource, /"build\.nextEffectLabel":\s*"Next Tier Effect"/);
  assert.match(moduleSource, /"build\.nextEffectLabel":\s*"다음 단계 효과"/);
  assert.match(moduleSource, /"build\.nextTierInline":\s*"Next tier in \{score\} pt"/);
  assert.match(moduleSource, /"build\.nextTierInline":\s*"\{score\} pt 후 다음 단계"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.smithing_modifier":\s*"Smithing results \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.smithing_modifier":\s*"대장 효율 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.alchemy_modifier":\s*"Alchemy results \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.alchemy_modifier":\s*"연금 효율 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.enchanting_modifier":\s*"Enchanting results \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.enchanting_modifier":\s*"부여 효율 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.sneaking_modifier":\s*"Sneak effectiveness \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.sneaking_modifier":\s*"은신 효율 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.lockpicking_modifier":\s*"Lockpicking effectiveness \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.lockpicking_modifier":\s*"자물쇠 효율 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.pickpocket_modifier":\s*"Pickpocket effectiveness \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.pickpocket_modifier":\s*"소매치기 효율 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.absorb_chance":\s*"Spell absorb chance \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.absorb_chance":\s*"주문 흡수 확률 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.stamina_rate":\s*"Stamina regen \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.stamina_rate":\s*"스태미나 재생 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.heal_rate":\s*"Health regen \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.heal_rate":\s*"체력 재생 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.magicka_rate":\s*"Magicka regen \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.magicka_rate":\s*"매지카 재생 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.destruction_modifier":\s*"Destruction cost reduction \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.destruction_modifier":\s*"파괴 주문 비용 감소 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.restoration_modifier":\s*"Restoration cost reduction \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.restoration_modifier":\s*"회복 주문 비용 감소 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.alteration_modifier":\s*"Alteration cost reduction \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.alteration_modifier":\s*"변화 주문 비용 감소 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.conjuration_modifier":\s*"Conjuration cost reduction \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.conjuration_modifier":\s*"소환 주문 비용 감소 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.illusion_modifier":\s*"Illusion cost reduction \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.illusion_modifier":\s*"환영 주문 비용 감소 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.shout_recovery_mult":\s*"Shout cooldown reduction \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.shout_recovery_mult":\s*"함성 재사용 감소 \+\{value\}%"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.melee_damage":\s*"Melee damage \+\{value\}"/);
  assert.match(moduleSource, /"build\.effectMagnitude\.melee_damage":\s*"근접 공격력 \+\{value\}"/);
  assert.match(moduleSource, /"build\.attack\.vitals\.title":\s*"Close Quarters"/);
  assert.match(moduleSource, /"build\.attack\.vitals\.description":\s*"\+2 melee damage while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.pinpoint\.title":\s*"Pinpoint"/);
  assert.match(moduleSource, /"build\.attack\.ferocity\.description":\s*"\+3% attack damage while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.pinpoint\.description":\s*"\+1\.5% critical chance while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.brawler\.title":\s*"Brawler"/);
  assert.match(moduleSource, /"build\.attack\.brawler\.description":\s*"\+0\.2 unarmed damage while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.reserve\.title":\s*"Reserve"/);
  assert.match(moduleSource, /"build\.attack\.reserve\.description":\s*"Gain \+8 max stamina and \+4% stamina regen while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.secondwind\.title":\s*"Second Wind"/);
  assert.match(moduleSource, /"build\.attack\.secondwind\.description":\s*"Stamina regenerates faster while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.destruction\.title":\s*"Destruction Adept"/);
  assert.match(moduleSource, /"build\.attack\.destruction\.description":\s*"Destruction spells cost less while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.endurance\.title":\s*"Endurance"/);
  assert.match(moduleSource, /"build\.defense\.endurance\.description":\s*"\+10 max stamina while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.bulwark\.title":\s*"Stalwart"/);
  assert.match(moduleSource, /"build\.defense\.bulwark\.description":\s*"\+8 max health while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.recovery\.title":\s*"Recovery"/);
  assert.match(moduleSource, /"build\.defense\.recovery\.description":\s*"Health regenerates faster while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.restoration\.title":\s*"Restoration"/);
  assert.match(moduleSource, /"build\.defense\.restoration\.description":\s*"Restoration spells cost less while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.reprisal\.title":\s*"Reprisal"/);
  assert.match(moduleSource, /"build\.defense\.reprisal\.description":\s*"A portion of incoming damage is reflected while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.alteration\.title":\s*"Alteration"/);
  assert.match(moduleSource, /"build\.defense\.alteration\.description":\s*"Alteration spells cost less while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.warding\.title":\s*"Magic Ward"/);
  assert.match(moduleSource, /"build\.defense\.warding\.description":\s*"Magic resistance increases while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.elementalWard\.title":\s*"Elemental Ward"/);
  assert.match(moduleSource, /"build\.defense\.elementalWard\.description":\s*"Fire, frost, and shock resistance increase while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.purification\.title":\s*"Purification Ward"/);
  assert.match(moduleSource, /"build\.defense\.purification\.description":\s*"Poison and disease resistance increase while this option is slotted\."/);
  assert.match(moduleSource, /"build\.defense\.absorption\.title":\s*"Absorption"/);
  assert.match(moduleSource, /"build\.defense\.absorption\.description":\s*"Spell absorption chance increases while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.smithing\.title":\s*"Smithing"/);
  assert.match(moduleSource, /"build\.utility\.smithing\.description":\s*"Smithing results improve while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.alchemy\.title":\s*"Alchemy"/);
  assert.match(moduleSource, /"build\.utility\.alchemy\.description":\s*"Alchemy crafting becomes more effective while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.enchanting\.title":\s*"Enchanting"/);
  assert.match(moduleSource, /"build\.utility\.enchanting\.description":\s*"Item enchanting becomes more effective while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.magicka\.title":\s*"Magicka Well"/);
  assert.match(moduleSource, /"build\.utility\.magicka\.description":\s*"Gain \+12 max magicka and \+2% magicka regen while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.meditation\.title":\s*"Meditation"/);
  assert.match(moduleSource, /"build\.utility\.meditation\.description":\s*"Gain \+8% magicka regen and \+2 max magicka while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.hauler\.title":\s*"Long Haul"/);
  assert.match(moduleSource, /"build\.utility\.hauler\.description":\s*"Gain \+6 max stamina and \+4 carry weight while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.mobility\.title":\s*"Mobility"/);
  assert.match(moduleSource, /"build\.utility\.mobility\.description":\s*"\+3% movement speed while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.wayfinder\.title":\s*"Wayfinder"/);
  assert.match(moduleSource, /"build\.utility\.wayfinder\.description":\s*"Stamina regenerates faster while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.echo\.title":\s*"Echo"/);
  assert.match(moduleSource, /"build\.utility\.echo\.description":\s*"Shout cooldown recovers faster while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.sneak\.title":\s*"Sneak"/);
  assert.match(moduleSource, /"build\.utility\.sneak\.description":\s*"You become harder to detect while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.lockpicking\.title":\s*"Lockpicking"/);
  assert.match(moduleSource, /"build\.utility\.lockpicking\.description":\s*"Locks become easier to pick while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.pickpocket\.title":\s*"Pickpocket"/);
  assert.match(moduleSource, /"build\.utility\.pickpocket\.description":\s*"Pickpocket success chance increases while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.conjuration\.title":\s*"Conjuration"/);
  assert.match(moduleSource, /"build\.utility\.conjuration\.description":\s*"Conjuration spells cost less while this option is slotted\."/);
  assert.match(moduleSource, /"build\.utility\.illusion\.title":\s*"Illusion"/);
  assert.match(moduleSource, /"build\.utility\.illusion\.description":\s*"Illusion spells cost less while this option is slotted\."/);
  assert.match(moduleSource, /"build\.attack\.vitals\.title":\s*"근접전"/);
  assert.match(moduleSource, /"build\.attack\.vitals\.description":\s*"슬롯 활성 시 근접 공격력이 2 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.pinpoint\.title":\s*"정조준"/);
  assert.match(moduleSource, /"build\.attack\.ferocity\.description":\s*"슬롯 활성 시 공격 피해가 3% 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.pinpoint\.description":\s*"슬롯 활성 시 치명타 확률이 1\.5% 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.brawler\.title":\s*"투사"/);
  assert.match(moduleSource, /"build\.attack\.brawler\.description":\s*"슬롯 활성 시 맨주먹 공격력이 0\.2 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.reserve\.title":\s*"기력"/);
  assert.match(moduleSource, /"build\.attack\.reserve\.description":\s*"슬롯 활성 시 최대 스태미나가 8, 스태미나 재생이 4% 증가합니다\."/);
  assert.match(moduleSource, /"build\.attack\.secondwind\.title":\s*"투지"/);
  assert.match(moduleSource, /"build\.attack\.precision\.title":\s*"속공"/);
  assert.match(moduleSource, /"build\.attack\.secondwind\.description":\s*"슬롯 활성 시 스태미나가 더 빠르게 회복됩니다\."/);
  assert.match(moduleSource, /"build\.attack\.destruction\.title":\s*"파괴숙련"/);
  assert.match(moduleSource, /"build\.attack\.destruction\.description":\s*"슬롯 활성 시 파괴 주문 비용이 감소합니다\."/);
  assert.match(moduleSource, /"build\.defense\.endurance\.title":\s*"인내"/);
  assert.match(moduleSource, /"build\.defense\.endurance\.description":\s*"슬롯 활성 시 최대 스태미나가 10 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.bulwark\.title":\s*"강건"/);
  assert.match(moduleSource, /"build\.defense\.bulwark\.description":\s*"슬롯 활성 시 최대 체력이 8 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.recovery\.title":\s*"회복력"/);
  assert.match(moduleSource, /"build\.defense\.guard\.title":\s*"철갑"/);
  assert.match(moduleSource, /"build\.defense\.bastion\.title":\s*"철벽"/);
  assert.match(moduleSource, /"build\.defense\.recovery\.description":\s*"슬롯 활성 시 체력이 더 빠르게 회복됩니다\."/);
  assert.match(moduleSource, /"build\.defense\.restoration\.title":\s*"회복"/);
  assert.match(moduleSource, /"build\.defense\.restoration\.description":\s*"슬롯 활성 시 회복 주문 비용이 감소합니다\."/);
  assert.match(moduleSource, /"build\.defense\.reprisal\.title":\s*"반격"/);
  assert.match(moduleSource, /"build\.defense\.reprisal\.description":\s*"슬롯 활성 시 일부 피해를 되돌려줍니다\."/);
  assert.match(moduleSource, /"build\.defense\.alteration\.title":\s*"변화"/);
  assert.match(moduleSource, /"build\.defense\.alteration\.description":\s*"슬롯 활성 시 변화 주문 비용이 감소합니다\."/);
  assert.match(moduleSource, /"build\.defense\.warding\.title":\s*"마법결계"/);
  assert.match(moduleSource, /"build\.defense\.warding\.description":\s*"슬롯 활성 시 마법 저항이 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.elementalWard\.title":\s*"원소방벽"/);
  assert.match(moduleSource, /"build\.defense\.elementalWard\.description":\s*"슬롯 활성 시 화염, 냉기, 전격 저항이 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.purification\.title":\s*"정화결계"/);
  assert.match(moduleSource, /"build\.defense\.purification\.description":\s*"슬롯 활성 시 독과 질병 저항이 증가합니다\."/);
  assert.match(moduleSource, /"build\.defense\.absorption\.title":\s*"주문흡수"/);
  assert.match(moduleSource, /"build\.defense\.absorption\.description":\s*"슬롯 활성 시 주문 흡수 확률이 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.smithing\.title":\s*"대장"/);
  assert.match(moduleSource, /"build\.utility\.smithing\.description":\s*"슬롯 활성 시 대장 작업의 품질이 향상됩니다\."/);
  assert.match(moduleSource, /"build\.utility\.alchemy\.title":\s*"연금"/);
  assert.match(moduleSource, /"build\.utility\.alchemy\.description":\s*"슬롯 활성 시 연금 제작의 효율이 향상됩니다\."/);
  assert.match(moduleSource, /"build\.utility\.enchanting\.title":\s*"부여"/);
  assert.match(moduleSource, /"build\.utility\.enchanting\.description":\s*"슬롯 활성 시 아이템 부여의 효율이 향상됩니다\."/);
  assert.match(moduleSource, /"build\.utility\.magicka\.title":\s*"마력"/);
  assert.match(moduleSource, /"build\.utility\.magicka\.description":\s*"슬롯 활성 시 최대 매지카가 12, 매지카 재생이 2% 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.meditation\.title":\s*"명상"/);
  assert.match(moduleSource, /"build\.utility\.meditation\.description":\s*"슬롯 활성 시 매지카 재생이 8%, 최대 매지카가 2 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.hauler\.title":\s*"강행군"/);
  assert.match(moduleSource, /"build\.utility\.hauler\.description":\s*"슬롯 활성 시 최대 스태미나가 6, 소지 한도가 4 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.mobility\.title":\s*"기동"/);
  assert.match(moduleSource, /"build\.utility\.mobility\.description":\s*"슬롯 활성 시 이동 속도가 3% 증가합니다\."/);
  assert.match(moduleSource, /"build\.utility\.wayfinder\.title":\s*"길잡이"/);
  assert.match(moduleSource, /"build\.utility\.wayfinder\.description":\s*"슬롯 활성 시 스태미나가 더 빠르게 회복됩니다\."/);
  assert.match(moduleSource, /"build\.utility\.echo\.title":\s*"메아리"/);
  assert.match(moduleSource, /"build\.utility\.echo\.description":\s*"슬롯 활성 시 함성 재사용 대기시간이 더 빨리 줄어듭니다\."/);
  assert.match(moduleSource, /"build\.utility\.sneak\.title":\s*"은신"/);
  assert.match(moduleSource, /"build\.utility\.sneak\.description":\s*"슬롯 활성 시 들키기 어려워집니다\."/);
  assert.match(moduleSource, /"build\.utility\.lockpicking\.title":\s*"자물쇠"/);
  assert.match(moduleSource, /"build\.utility\.lockpicking\.description":\s*"슬롯 활성 시 자물쇠 해제가 쉬워집니다\."/);
  assert.match(moduleSource, /"build\.utility\.pickpocket\.title":\s*"소매치기"/);
  assert.match(moduleSource, /"build\.utility\.pickpocket\.description":\s*"슬롯 활성 시 소매치기 성공률이 높아집니다\."/);
  assert.match(moduleSource, /"build\.utility\.conjuration\.title":\s*"소환"/);
  assert.match(moduleSource, /"build\.utility\.conjuration\.description":\s*"슬롯 활성 시 소환 주문 비용이 감소합니다\."/);
  assert.match(moduleSource, /"build\.utility\.illusion\.title":\s*"환영"/);
  assert.match(moduleSource, /"build\.utility\.illusion\.description":\s*"슬롯 활성 시 환영 주문 비용이 감소합니다\."/);
  assert.match(moduleSource, /"build\.utility\.barter\.description":\s*"슬롯 활성 시 상점 구매\/판매 가격이 10% 유리해집니다\."/);
  assert.match(moduleSource, /return base\.replace/);
});

test("translator normalizes language and formats variables", () => {
  const translator = mod.createTranslator({ getLanguage: () => "ko" });
  assert.equal(translator.t("btn.save"), "저장");
  assert.equal(translator.t("build.attack.pinpoint.title"), "정조준");
  assert.equal(translator.t("build.attack.reserve.title"), "기력");
  assert.equal(translator.t("build.defense.warding.title"), "마법결계");
  assert.equal(translator.t("build.defense.elementalWard.title"), "원소방벽");
  assert.equal(translator.t("build.defense.purification.title"), "정화결계");
  assert.equal(translator.t("build.defense.recovery.title"), "회복력");
  assert.equal(translator.t("build.utility.smithing.title"), "대장");
  assert.equal(translator.t("build.utility.echo.title"), "메아리");
  assert.equal(translator.t("build.attack.precision.title"), "속공");
  assert.equal(translator.t("build.attack.destruction.title"), "파괴숙련");
  assert.equal(translator.t("build.defense.guard.title"), "철갑");
  assert.equal(translator.t("build.defense.bastion.title"), "철벽");
  assert.equal(translator.t("build.defense.bulwark.title"), "강건");
  assert.equal(translator.t("build.utility.wayfinder.description"), "슬롯 활성 시 스태미나가 더 빠르게 회복됩니다.");
  assert.equal(translator.tFmt("rewards.more", "+{n}개 더", { n: 5 }), "+5개 더");
  assert.equal(mod.normalizeLanguage("jp"), "en");
});
